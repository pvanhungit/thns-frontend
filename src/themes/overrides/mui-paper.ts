import { type Theme } from '@mui/material/styles';

// ----------------------------------------------------------------------

export const MuiPaper = (theme: Theme) => ({
  MuiPaper: {
    defaultProps: {
      elevation: 0,
    },
  },
});
